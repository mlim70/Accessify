{
  "name": "emoryhax",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd site && npm run dev\"",
    "install-all": "npm install && cd backend && npm install && cd ../site && npm install"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.310",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "next": "^15.2.3"
  }
}
